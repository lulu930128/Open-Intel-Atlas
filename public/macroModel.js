export const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
export function valueLabel(value, unit, semantics) { return value === null || value === undefined || !Number.isFinite(value) ? "未取得" : `${value}${semantics?.value_suffix ?? (unit === "percent" ? "%" : "")}`; }
export function timeLabel(value) { return !value || !Number.isFinite(Date.parse(value)) ? "未提供" : new Intl.DateTimeFormat("zh-TW",{timeZone:"Asia/Taipei",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(value)); }
export function statusLabel(value) { return ({current:"資料有效",complete:"已齊備",partial:"部分資料",stale:"資料逾期",missing:"尚未取得",disabled:"未啟用",degraded:"來源異常",unknown:"尚無法確認",scheduled:"尚未公布",due:"已到預定時間",released:"已取得發布"})[value] || value || "尚無法確認"; }
export function basisLabel(item) { return item.display_semantics?.basis_label ?? `${({mom:"月增",yoy:"年增",index:"指數"})[item.transformation] || item.transformation} · ${({SA:"季調",NSA:"未季調"})[item.seasonal_adjustment] || item.seasonal_adjustment}${item.index_base ? ` · ${item.index_base}` : ""}`; }
export function sourceLink(url) { try { const u=new URL(url);return u.protocol === "https:" ? `<a href="${escapeHtml(u.href)}" target="_blank" rel="noopener noreferrer">官方原文 ↗</a>` : "未提供"; } catch { return "未提供"; } }
